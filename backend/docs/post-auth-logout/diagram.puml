@startuml activity diagram
:4.1. Идентификация пользователя по JWT;
if (JWT валиден?) then (да)
    :4.2. Валидация входных параметров;
    if (Метод POST?) then (да)
        :4.3. Формирование итогового ответа;
        #palegreen :4.4. Отправка ответа об успехе;
        stop
    else (нет)
    endif
else (нет)
endif
#pink :Отправка ответа об ошибке;
stop
@enduml
