@startuml activity diagram
:4.1. Валидация входных параметров;
if (Валидация прошла успешно?) then (да)
    :4.2. Запрос к БД на проверку уникальности email;
    if (Email свободен?) then (да)
        :4.3. Хэширование пароля (bcrypt, cost=12);
        if (Хэширование успешно?) then (да)
            :4.4. Запрос к БД на создание пользователя;
            if (Запись создана?) then (да)
                :4.5. Формирование итогового ответа;
                #palegreen :4.6. Отправка ответа об успехе;
                stop
            else (нет)
            endif
        else (нет)
        endif
    else (нет)
    endif
else (нет)
endif
#pink :Отправка ответа об ошибке;
stop
@enduml
