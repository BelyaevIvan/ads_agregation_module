@startuml activity diagram
:4.1. Идентификация пользователя по JWT;
if (JWT валиден?) then (да)
    :4.2. Валидация входных параметров;
    if (Валидация прошла успешно?) then (да)
        :4.3. Проверка существования объявления;
        if (Объявление найдено?) then (да)
            :4.4. Вставка записи в избранное (ON CONFLICT DO NOTHING);
            if (Запись создана впервые?) then (да)
                :4.5. Формирование итогового ответа;
                #palegreen :4.6. Отправка ответа об успехе (201 Created);
                stop
            else (нет — уже существовала)
                :4.5. Формирование итогового ответа;
                #palegreen :4.6. Отправка ответа об успехе (200 OK);
                stop
            endif
        else (нет)
        endif
    else (нет)
    endif
else (нет)
endif
#pink :Отправка ответа об ошибке;
stop
@enduml
