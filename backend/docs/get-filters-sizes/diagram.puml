@startuml activity diagram
:4.1. Проверка in-memory кэша;
if (Кэш свежий?) then (да)
    #palegreen :Вернуть результат из кэша;
    stop
else (нет)
    :4.2. Параллельные запросы к БД;
    if (Все запросы успешны?) then (да)
        :4.3. Сортировка (численная, с фолбэком на лексикографику);
        :4.4. Запись в кэш (TTL 5 минут);
        :4.5. Формирование итогового ответа;
        #palegreen :4.6. Отправка ответа об успехе;
        stop
    else (нет)
    endif
endif
#pink :Отправка ответа об ошибке;
stop
@enduml
