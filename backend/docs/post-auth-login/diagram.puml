@startuml activity diagram
:4.1. Валидация входных параметров;
if (Валидация прошла успешно?) then (да)
    :4.2. Запрос к БД на получение пользователя по email;
    if (Пользователь найден?) then (да)
        :4.3. Сравнение пароля с хэшем (bcrypt);
        if (Пароль совпал?) then (да)
            :4.4. Формирование и подписание JWT;
            if (Токен подписан?) then (да)
                :4.5. Формирование итогового ответа;
                #palegreen :4.6. Отправка ответа об успехе;
                stop
            else (нет)
            endif
        else (нет)
        endif
    else (нет)
    endif
else (нет)
endif
#pink :Отправка ответа об ошибке;
stop
@enduml
